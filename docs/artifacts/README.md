# Artifacts

Exports from skills (PRDs, triage write-ups, plans, issue bodies). Filenames use the step prefix when tied to a snapshot (e.g. `01-write-a-prd-*.md`).

Replace placeholder content with real output after you run the corresponding skill in your agent.
